{% extends 'home.html.twig' %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block header %}
    <h3 class="page-title">Tender i ri</h3>
{% endblock %}
{% block bodymain %}



    <div class=" panel modifiko detaje d-flex justify-content-center">

    {{ form_start(form) }}
        {{ form_widget(form) }}
    <button type="submit" class="btn btn-primary">
        {{ button_label|default('Publiko') }}
    </button>

{{ form_end(form) }}
    </div>


    <script src="{{asset('bootstrap-datepicker/js/bootstrap-datepicker.min.js')}}"></script>

    <script>
    var header = document.getElementById("krijoTender");
    header=header.classList.add('active');
</script>

{% endblock %}